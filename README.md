# TensorFlow ImageDataGenerator CNN - Happy or Sad Emojis

A convolutional neural network trained with Keras's `ImageDataGenerator` to classify a small dataset of happy vs. sad emoji-style faces, with training stopped automatically via a custom callback once the model reaches 99.9% training accuracy.
